# Never commit secrets — this repo only references the file, never contains it.
*.env
.secrets/
agent-sandbox.env

# Build output from scripts/build-release.sh
dist/
# npm publish staging (artifact is dropped here by CI, never committed)
npm/bin/

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
